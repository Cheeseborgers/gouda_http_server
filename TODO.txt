// TODO: Fix file cache lru and how logging is handled with in the cache. figure a better way to pass the req and conn id's
// For the routes and client handler


// TODO: Why are we not logging port

// FIX: Ranges on dls
// TODO: fix 404 response rendering on browsers. currently renders html text rather than rendered









Connection Throttling (Optional):Limit concurrent WebSocket connections in HttpServer or ClientHandler to prevent resource exhaustion.
Requires tracking active connections (e.g., in a std::unordered_set<ConnectionId>).



